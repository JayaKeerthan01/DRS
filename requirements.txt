Flask==3.1.3
Werkzeug==3.1.7
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3
requests>=2.31.0

# Everything above is all that's required to run this project as shipped.
# CSRF protection, RBAC, and login rate limiting are implemented by hand in
# app.py/database/db.py using only the standard library, specifically so
# the project stays zero-extra-dependency. If you outgrow the hand-rolled
# versions, these are the standard replacements:
#
# Flask-WTF        # drop-in CSRF (this project implements it manually)
# Flask-Limiter    # more robust rate limiting than the login_attempts table
# gunicorn         # production WSGI server — never use `python app.py`'s
#                  # dev server (app.run()) outside local development
#
# For real-time push instead of the current 15s polling, Flask-SocketIO +
# eventlet/gevent is the natural next step (see README "Roadmap").
#
# anthropic>=0.40.0   # optional — only if ANTHROPIC_API_KEY is set, for the
#                     # citizen chatbot's free-form mode. The rule-based
#                     # matcher (agents/citizen_chat_agent.py) works fully
#                     # without it — this is an upgrade, not a requirement.
